///|
pub fn max_int(left : Int, right : Int) -> Int {
  if left > right {
    left
  } else {
    right
  }
}

///|
pub fn min_int(left : Int, right : Int) -> Int {
  if left < right {
    left
  } else {
    right
  }
}
