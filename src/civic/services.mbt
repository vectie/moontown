///|
fn civic_service(
  id~ : String,
  title~ : String,
  service_kind~ : String,
  module_mode~ : String,
  book_role~ : String,
  skill_mode~ : String,
  purpose~ : String,
  workflow~ : Array[String],
  agent_work~ : Array[String],
  place_work~ : Array[String],
  schema_pages~ : Array[String],
  wiki_pages~ : Array[String],
  review_queues~ : Array[String],
  skill_pack~ : String,
  output_contract~ : String,
  worker_role~ : @core.WorkerRole,
  acceptance~ : Array[String],
  projection_summary~ : String,
) -> CivicServiceDefinition {
  let book_id = "wenyu-\{id}"
  {
    id,
    title,
    book_id,
    service_kind,
    module_mode,
    persistence_mode: civic_persistence_mode(module_mode),
    book_role,
    skill_mode,
    purpose,
    workflow,
    agent_work,
    place_work,
    schema_pages,
    wiki_pages,
    review_queues,
    skill_pack,
    skill_path: "skills/\{skill_pack}/SKILL.md",
    output_contract,
    worker_role,
    target_pages: unique_strings(
      [
        "raw/bootstrap/CIVIC_SERVICE_CONTRACT.md",
        "raw/bootstrap/BUILDING_PROTOCOL_CONTRACT.md",
        "skills/wenyu-civic-service/SKILL.md",
        "skills/\{skill_pack}/SKILL.md",
        ..schema_pages,
        ..wiki_pages,
        ..review_queues,
        "wiki/ledgers/service-ledger.md",
        "wiki/ledgers/exchange-ledger.md",
        "wiki/history/service-history.md",
        "book/moonbook-ui-state.json",
        "book/site/generated/index.html",
      ],
    ),
    acceptance,
    projection_summary,
  }
}
