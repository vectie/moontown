{
  "scripts": {
    "dev": "vite",
    "build": "node scripts/prepare-rabbita-build.mjs && vite build"
  },
  "type": "module",
  "dependencies": {
    "@rabbita/vite": "^0.2.0",
    "vite": "^7.3.2"
  }
}
